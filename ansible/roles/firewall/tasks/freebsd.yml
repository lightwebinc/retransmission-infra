---
- name: Deploy pf ruleset for retry-endpoint
  ansible.builtin.template:
    src: retry-endpoint.pf.conf.j2
    dest: /etc/pf.d/retry-endpoint.conf
    owner: root
    group: wheel
    mode: "0600"
  notify: Reload pf

- name: Include retry-endpoint rules in main pf.conf
  ansible.builtin.lineinfile:
    path: /etc/pf.conf
    regexp: '^include "/etc/pf.d/retry-endpoint.conf"'
    line: 'include "/etc/pf.d/retry-endpoint.conf"'
    create: true
    mode: "0600"
  notify: Reload pf

- name: Ensure pf is enabled
  ansible.builtin.lineinfile:
    path: /etc/rc.conf
    regexp: '^pf_enable='
    line: 'pf_enable="YES"'
  notify: Reload pf
